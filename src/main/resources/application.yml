spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/learn
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
  amqp:
    deserialization:
      trust:
        all: true



logging:
  level:
    root: INFO
    com.tapiwa.demo: DEBUG